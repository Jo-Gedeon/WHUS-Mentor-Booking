node_modules/
.env
.env.*
!.env.example

# Service account keys / other Google credential JSON files —
# only package.json and package-lock.json are allowed through.
*.json
!package.json
!package-lock.json
